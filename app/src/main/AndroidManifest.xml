<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.material_design_app">

    <application
        android:allowBackup="true"
        android:icon="@drawable/avatar"
        android:label="@string/app_name"
        android:roundIcon="@drawable/avatar"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mainScreens.ui.main.NavigationClass"></activity>
        <activity
            android:name=".mainScreens.ui.main.MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>